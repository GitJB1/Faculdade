#include <iostream>
#include <cmath>

using namespace std;

int main(){

    int a = 5, b = 10, c = -8;
    double d = 1.5;

    cout << 2 * a % 3 - c << endl;
    cout << sqrt(-2 * c) / 4;
    cout << ((20/3)/3) + pow(8, 2)/2 << endl;
    cout << (30 % 4 * pow(3, 3)) * -1 << endl;
    cout << pow(-c, 2) + (d * 10) / a << endl;
    cout << sqrt(pow(a, b/a)) + c * d << endl;

}